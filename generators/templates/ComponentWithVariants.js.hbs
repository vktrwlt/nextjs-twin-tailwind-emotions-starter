import PropTypes from 'prop-types';
import tw, { styled, css, theme, TwStyle } from 'twin.macro';

const primaryVariants = {
  // Named class sets
  primary: tw` `,
  secondary: tw` `,
};

const sizeVariants = {
  // Named class sets
  sm: tw` `,
  md: tw` `,
  lg: tw` `,
};

export const {{pascalCase name}} = styled.div(({ variant = 'primary', size = 'md' }) => [
    // Base Styles
    tw` `,

    // Dynamic Styles
    primaryVariants[variant],
    sizeVariants[size],
  ]
);

{{pascalCase name}}.defaultProps = {
  variant: 'primary',
  size: 'md',
};

{{pascalCase name}}.propTypes = {
  variant: PropTypes.oneOf(['primary', 'secondary']),
  size: PropTypes.oneOf(['sm', 'md', 'lg']),
};